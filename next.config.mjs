/** @type {import('next').NextConfig} */
// import { withContentLayer } from 'next-contentlayer';
import * as nextContentlayer from 'next-contentlayer';

const nextConfig = {};

export default nextContentlayer.withContentlayer(nextConfig);
